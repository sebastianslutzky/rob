@page "/"
@page "/object/{resource}"

@using Microsoft.Extensions.Logging
@using RestfulObjectApi.Representation.Types
@using Blazor.Extensions.Logging;
@using rob.Pages.ListView;
@using rob.Services;
@using rob.Pages.ObjectViews;

@if (FocusObject != null)
{
    //TODO: move conditionals to code behind to simplify this template
    if (FocusObjectAsIsisObject != null)
    {
        <IsisList Context="FocusObjectAsIsisObject" />
    }
    else
   if (FocusObjectAsIsisSingleObject != null & ObjectLayout != null)
    {
        <ObjectView Context="FocusObjectAsIsisSingleObject"  Layout="ObjectLayout" />
    }
}

